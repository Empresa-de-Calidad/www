function openIndex() {
        window.location.replace("index.html");
}

var navSearchStatus = false;

function nav_search_menu() {
        if(navSearchStatus == false) navSearchStatus == true;
        else navSearchStatus == false;

        extension = document.nav_search_menu;
        
        
}